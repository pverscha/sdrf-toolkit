/** Bump this when the index format or pipeline logic changes in a way that requires consumers to re-download. */
export const INDEX_VERSION = "1.0.1";

/** Bump this when the index file or manifest schema changes in a backwards-incompatible way. */
export const SCHEMA_VERSION = "1.0.0";
